{{ $module = $dist->name =~ s/-/::/g; }}
{{ $path = $dist->name =~ s/-/\//g; }}

[TemplateModule / :DefaultModuleMaker]
template = AppKitX.pm

[GatherDir / AppKitX]
root=skel/AppKitX
prefix=lib/{{ $path }}


[GenerateFile / Controller]
filename={{ $dist->main_module }}
content = package {{ $module }}::Controller::{{ (split /::/, $module)[-1] }};
content = 
content = use Moose;
content = use namespace::autoclean;
content = BEGIN { extends 'Catalyst::Controller::HTML::FormFu'; };
content = with 'OpusVL::AppKit::RolesFor::Controller::GUI';
content = 1;
